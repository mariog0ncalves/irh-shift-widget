{
  "name": "irh-shift-widget",
  "version": "1.0.0",
  "description": "iRH Shift Widget Extension",
  "license": "ISC",
  "author": "drajacinta@imed.pt",
  "type": "commonjs",
  "main": "content.js",
  "scripts": {
    "build:chrome": "rm -rf manifest.json && cp chrome-manifest.json manifest.json  -f",
    "build:firefox": "rm -rf manifest.json && cp ff-manifest.json manifest.json -f && rm -rf irh-counter.xpi && zip -r irh-counter.xpi *"
  }
}
